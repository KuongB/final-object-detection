# =============================================================================
# Fruit & Vegetable Detection - Python dependencies
# Verified on: Windows 11, Python 3.11.15, NVIDIA RTX 4060 Laptop (8 GB, sm_89)
# =============================================================================
#
# STEP 1 - install PyTorch first, from the CUDA wheel index (NOT plain PyPI,
#          which ships the CPU-only build):
#
#   pip install torch==2.13.0 torchvision==0.28.0 \
#       --index-url https://download.pytorch.org/whl/cu126
#
# STEP 2 - install everything below:
#
#   pip install -r requirements.txt
#
# On Google Colab / Kaggle, torch + torchvision are preinstalled - skip step 1
# and only install fiftyone / ultralytics / pycocotools.
# -----------------------------------------------------------------------------

torch==2.13.0
torchvision==0.28.0

# --- dataset construction ---
fiftyone==1.20.1
pycocotools==2.0.11

# --- models ---
ultralytics==8.4.117          # YOLO11s
transformers==5.15.0          # D-FINE-N (ustc-community/dfine-nano-coco)
timm==1.0.28                  # backbone registry transformers pulls D-FINE through

# --- scientific stack ---
numpy==2.4.4
pandas==3.0.5
matplotlib==3.11.1
seaborn==0.13.2
opencv-python==5.0.0.93
Pillow==12.2.0
tqdm==4.70.0
PyYAML==6.0.3

# --- web application ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
jinja2==3.1.6
aiofiles==25.1.0

# --- notebooks ---
jupyterlab==4.6.2
ipykernel==7.3.0
