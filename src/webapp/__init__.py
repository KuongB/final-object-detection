"""The web application - Requirement 2 of the final project.

Everything the server actually *does* lives here; `scripts/30_serve.py` only
starts it, and `webapp/` holds the static assets the browser downloads. That
split is the same one `src/training` and `src/evaluation` already use.
"""
